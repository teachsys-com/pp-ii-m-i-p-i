# Corre los siguientes comandos previo usar este recurso

```
git remote -v # Validar que estamos en el repo del profe

git checkout <BRANCH> # Moverte a la branch donde trabajaste

git pull origin main-category # Traemos los nuevos .md para trabajar
```